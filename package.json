{
  "name": "project-status-testing",
  "version": "1.0.0",
  "description": "Testing harness for Project Status web application",
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest --reporter=verbose",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "jsdom": "^23.0.0",
    "@vitest/ui": "^1.0.0"
  },
  "dependencies": {
    "node-fetch": "^3.3.0"
  }
}
